# Система распознавания автомобильных номеров 

**Веб-приложение для автоматического детектирования и распознавания автомобильных номеров на изображениях с использованием современных методов компьютерного зрения и глубокого обучения.**

---

## ✨ Основные возможности

- **Загрузка изображений** через удобный веб-интерфейс
- **Автоматическое детектирование** номерных знаков с помощью YOLOv11n
- **Распознавание текста** с использованием трансформерной модели TrOCR
- **Визуализация результатов** с bounding boxes и распознанным текстом
- **Статистика обработки** и подробная информация по каждому найденному номеру
- **Система логирования** всех операций распознавания

---

## 🛠️ Технологический стек

### Backend
- **FastAPI** — асинхронный веб-фреймворк с автоматической документацией OpenAPI
- **Ultralytics YOLO** — детекция объектов (YOLOv11n)
- **Hugging Face Transformers** — распознавание текста (TrOCR Large Printed)
- **PyTorch** — фреймворк глубокого обучения
- **OpenCV** — обработка и визуализация изображений

### Frontend
- **Jinja2** — шаблонизация HTML
- **Чистый CSS/JavaScript** — адаптивный интерфейс
- **Bootstrap** элементы для стилизации

---

## Структура проекта

```
project/
├── app.py                 # Основное приложение FastAPI
├── detection.py           # Класс LicensePlateDetector (ядро системы)
├── run.py                # Скрипт запуска сервера
├── requirements.txt       # Зависимости проекта
│
├── templates/            # HTML шаблоны
│   ├── detection.html    # Главная страница загрузки
│   ├── result.html       # Страница с результатами
│   └── logs.html         # Страница логов
│
├── static/               # Статические файлы
│   └── uploads/          # Загруженные и обработанные изображения
│
├── plate_training/       # Результаты обучения YOLO модели
│   └── yolo11n/
│       └── weights/
│           └── best.pt   # Обученная модель детекции
│
└── datasets/             # Размеченный датасет для обучения
    ├── images/
    │   ├── train/
    │   └── val/
    └── labels/
        ├── train/
        └── val/
```

---



## Результаты модели

Обученная модель детекции показала следующие метрики на валидационной выборке:

| Метрика | Значение | Описание |
|---------|----------|----------|
| **Precision** | 0.85 | Высокая точность детекции |
| **Recall** | 0.80 | Хорошая полнота обнаружения |
| **mAP@0.5** | 0.75 | Общая эффективность модели |

---

## 🔧 API Endpoints

### Основные endpoints:
- `GET /` — Главная страница с формой загрузки
- `POST /detect` — Обработка загруженного изображения
- `GET /logs` — Просмотр истории распознаваний

---



## 🎯 Особенности реализации

### 1. **Двухэтапный пайплайн обработки**
```python
# 1. Детекция с помощью YOLO
results = yolo_model(image, conf=0.3)

# 2. OCR с помощью TrOCR
plate_text = trocr_model.generate(plate_crop)
```

### 2. **Постобработка результатов**
- Транслитерация латинских символов в кириллические
- Валидация по шаблонам российских номеров
- Фильтрация шумовых символов

### 3. **Адаптивная архитектура**
```python
# Автоматический выбор устройства для вычислений
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
# Модели автоматически используют GPU при наличии
```






*✨ Спасибо за интерес к проекту! Если система оказалась полезной, поставьте звезду ⭐ на GitHub!*
